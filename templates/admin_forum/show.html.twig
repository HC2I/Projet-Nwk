{% extends 'base.html.twig' %}

{% block title %}Forum
{% endblock %}

{% block body %}
	<h1>Forum</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ forum.id }}</td>
			</tr>
			<tr>
				<th>Titre</th>
				<td>{{ forum.Titre }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ forum.Description }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_admin_forum_index') }}">Retour to list</a>

	<a href="{{ path('app_admin_forum_edit', {'id': forum.id}) }}">Editer</a>

	{{ include('admin_forum/_delete_form.html.twig') }}
{% endblock %}
